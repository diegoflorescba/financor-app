services:
  - type: web
    name: financor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn prestamos_app.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: FLASK_APP
        value: prestamos_app.app
      - key: FLASK_ENV
        value: production 